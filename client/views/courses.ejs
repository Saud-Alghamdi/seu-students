<html dir="rtl">
  <head>
    <%- include('partials/head.ejs') %>
    <title>المواد</title>
  </head>
  <body class="d-flex flex-column">
    <%- include('partials/nav.ejs') %>

    <main class="departments-page-container__main container position-relative flex-grow-1 text-center text-light my-4">
      <!-- Back link -->
      <a class="text-light text-decoration-none position-absolute" id="back-link" onclick="history.back()">
        <i class="fa-sharp fa-solid fa-arrow-right-long"></i>
      </a>

      <header>
        <% if(depAbbr === 'CS') { %>
        <h1><%=langData.COMPUTER_SCIENCE_COURSES%></h1>
        <% } else if(depAbbr === 'IT') { %>
        <h1><%=langData.INFORMATION_TECHNOLOGY_COURSES%></h1>
        <% } else if(depAbbr === 'DS') { %>
        <h1><%=langData.DATA_SCIENCE_COURSES%></h1>
        <% } %>
      </header>

      <div class="departments-flex-container mt-5 d-flex justify-content-center flex-wrap">
        <% if(typeof courses !== 'undefined') { %> <% courses.forEach(course => { %>

        <div class="departments-page-container__div-box border-dark rounded-5 p-2 m-1">
          <a class="department-link d-flex justify-content-center align-items-center text-light fs-3" href="./<%=course.code%>/posts"><%= course.code %></a>
        </div>

        <% }) %> <% } %>
      </div>
    </main>

    <%- include('partials/footer.ejs') %>
  </body>
</html>

<nav class="navbar navbar-expand-md bg-light py-0">
  <div class="container-fluid">
    <a class="navbar-brand m-0" href="/">
      <img src="/images/seu-logo.png" alt="Placeholder" width="50" height="50" class="navbar-logo d-inline-block align-text-top" />
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav d-flex align-items-md-center p-0">
        <li class="nav-item px-md-2">
          <a class="nav-link" aria-current="page" href="/"><%=langData.HOME%></a>
        </li>
        <li class="nav-item px-md-2">
          <a class="nav-link" href="/departments"><%=langData.DEPARTMENTS%></a>
        </li>
        <!-- if Logged in -->
        <% if(typeof user !== 'undefined') { %>
        <li class="nav-item px-md-2">
          <a class="nav-link" href="/logout"><%=langData.LOGOUT%></a>
        </li>
        <li class="nav-item px-md-2 d-flex align-items-center">
          <a class="nav-link" href="/dashboard" style="padding-bottom: 0">
            <%=user.username%> <% if (user.gender === 'male') { %>
            <img src="/images/male-icon.svg" alt="Placeholder" width="30" height="30" style="padding-bottom: 3px" />
            <% } else if (user.gender === 'female') { %>
            <img src="/images/female-icon.svg" alt="Placeholder" width="30" height="30" style="padding-bottom: 3px" />
            <% } %>
          </a>
        </li>
        <!-- if NOT Logged in -->
        <% } else { %>
        <li class="nav-item px-md-2">
          <a class="nav-link" href="/login"><%=langData.LOGIN%></a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/signup"><%=langData.SIGNUP%></a>
        </li>
        <% } %>
        <li class="nav-item">
          <a class="nav-link" href="?lang=ar">العربية</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="?lang=en">English</a>
        </li>
      </ul>
    </div>
  </div>
</nav>
